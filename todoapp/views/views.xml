<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="todoapp.list">
      <field name="name">todoapp list</field>
      <field name="model">todoapp.todoapp</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="todoapp.action_window">
      <field name="name">todoapp window</field>
      <field name="res_model">todoapp.todoapp</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="todoapp.action_server">
      <field name="name">todoapp server</field>
      <field name="model_id" ref="model_todoapp_todoapp"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="todoapp" id="todoapp.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="todoapp.menu_1" parent="todoapp.menu_root"/>
    <menuitem name="Menu 2" id="todoapp.menu_2" parent="todoapp.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="todoapp.menu_1_list" parent="todoapp.menu_1"
              action="todoapp.action_window"/>
    <menuitem name="Server to list" id="todoapp" parent="todoapp.menu_2"
              action="todoapp.action_server"/>
    -->
  </data>
</odoo>